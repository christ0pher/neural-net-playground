# The neural net playground

A small script which can be used to recognize and predict different kinds of data.

# Example models

There is an example which was trained with handwritten numbers from kaggle: https://www.kaggle.com/c/digit-recognizer/data. The model has an accuracy of around 2% errorrate.
